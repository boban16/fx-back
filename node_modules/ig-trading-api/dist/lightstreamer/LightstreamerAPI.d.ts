import { LightstreamerClient, Subscription } from 'lightstreamer-client-node';
import { Authorization } from '../client';
import { CandleStick } from '../market';
import { ChartResolution } from './interfaces';
export declare class LightstreamerAPI {
    private readonly auth;
    lightstream?: LightstreamerClient;
    candleSubscription?: Subscription;
    constructor(auth: Authorization);
    private createLightStream;
    subscribeCandles(epicList: string[], resolution: ChartResolution, onCandleUpdate: (epic: string, candle: CandleStick) => void): LightstreamerClient;
}
